spring:
  application:
    name: pwb-backend
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  jpa:
    open-in-view: false

server:
  port: 8080
  forward-headers-strategy: framework

logging:
  level:
    com.pwb: INFO
    org.springframework: WARN
    org.hibernate: WARN
