import { LoginForm } from "@/features/auth/components/login-form";

export const metadata = {
  title: "Đăng nhập | PWB",
  description: "Đăng nhập vào tài khoản Play With Beats",
};

export default function LoginPage() {
  return <LoginForm />;
}
